---
title: "Rate Limiting"
description: "Entenda os limites de requisição da API Bob Payments, headers de rate limit e como lidar com HTTP 429"
---

# Rate Limiting

A API aplica limites de requisição por cliente (documento CPF/CNPJ) para prevenir abuso e garantir estabilidade.

## Como funciona

O rate limit é aplicado no endpoint de criação de transações (`POST /api/transactions`) por **documento do cliente** dentro de um projeto. Isso significa que cada cliente tem seu próprio contador de requisições.

Os limites são configuráveis por projeto via dashboard.

## Headers de resposta

Toda resposta do endpoint de transações inclui headers de rate limit:

| Header | Tipo | Descrição |
|--------|------|-----------|
| `X-RateLimit-Limit` | `integer` | Número máximo de requisições permitidas na janela |
| `X-RateLimit-Remaining` | `integer` | Requisições restantes na janela atual |
| `X-RateLimit-Reset` | `integer` | Timestamp Unix (segundos) de quando a janela reseta |
| `Retry-After` | `integer` | Segundos para aguardar (presente apenas em respostas 429) |

## Quando o limite é excedido

Se o limite for ultrapassado, a API retorna HTTP `429` com o erro `ERR_RATE_LIMIT_001`:

```json
{
  "type": "ERR_RATE_LIMIT_001",
  "title": "Rate Limit Excedido",
  "status": 429,
  "detail": "Muitas requisições. Tente novamente em 30 segundos.",
  "instance": "/api/transactions"
}
```

## Tratando rate limit

<CodeGroup>

```javascript Node.js
async function createTransaction(data, retries = 3) {
  const response = await fetch('https://api.payments.bob.company/api/transactions', {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer sk_test_xxx',
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(data),
  });

  if (response.status === 429 && retries > 0) {
    const retryAfter = parseInt(response.headers.get('Retry-After') || '30');
    await new Promise(resolve => setTimeout(resolve, retryAfter * 1000));
    return createTransaction(data, retries - 1);
  }

  return response.json();
}
```

```python Python
import requests
import time

def create_transaction(data, retries=3):
    response = requests.post(
        'https://api.payments.bob.company/api/transactions',
        headers={'Authorization': 'Bearer sk_test_xxx'},
        json=data,
    )

    if response.status_code == 429 and retries > 0:
        retry_after = int(response.headers.get('Retry-After', 30))
        time.sleep(retry_after)
        return create_transaction(data, retries - 1)

    return response.json()
```

```bash cURL
# Verifique o header Retry-After em respostas 429
curl -X POST https://api.payments.bob.company/api/transactions \
  -H "Authorization: Bearer sk_test_xxx" \
  -H "Content-Type: application/json" \
  -d '{"customer": {...}, "payment": {...}}' \
  -w "\nRate-Limit: %{header:X-RateLimit-Remaining}/%{header:X-RateLimit-Limit}\n"
```

</CodeGroup>

## Boas práticas

<AccordionGroup>
  <Accordion title="Monitore os headers">
    Acompanhe `X-RateLimit-Remaining` para evitar atingir o limite. Implemente backoff proativo quando restar menos de 10% das requisições.
  </Accordion>
  <Accordion title="Use backoff exponencial">
    Em caso de 429, aguarde o tempo indicado em `Retry-After`. Se precisar implementar retry genérico, use backoff exponencial (1s, 2s, 4s, 8s...).
  </Accordion>
  <Accordion title="Não faça polling agressivo">
    Para verificar status de transações, use webhooks em vez de polling. Webhooks notificam em tempo real sem consumir sua cota de rate limit.
  </Accordion>
</AccordionGroup>
