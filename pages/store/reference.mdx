---
title: "Estrutura do projeto"
description: "Consulte as configurações e informações do seu projeto"
icon: "book-open-cover"
---

## Estrutura

A loja representa o projeto associado à sua chave de API:

```json
{
  "id": "clz9x8w7v6u5t4s3r2q1",
  "name": "Minha Loja",
  "devMode": false,
  "pixExpirationMinutes": 60,
  "maxTransactionAmountCents": null
}
```

## Atributos

<ResponseField name="id" type="string">
  Identificador único do projeto no formato CUID.
</ResponseField>

<ResponseField name="name" type="string">
  Nome do projeto configurado no Dashboard.
</ResponseField>

<ResponseField name="devMode" type="boolean">
  Indica se o projeto está em modo sandbox (`true`) ou produção (`false`). Reflete o ambiente da chave de API utilizada (`sk_test_*` ou `sk_live_*`).
</ResponseField>

<ResponseField name="pixExpirationMinutes" type="integer">
  Tempo em minutos até uma cobrança PIX expirar. Configurável no Dashboard.
</ResponseField>

<ResponseField name="maxTransactionAmountCents" type="integer | null">
  Valor máximo permitido por transação, em centavos. `null` indica que não há limite configurado.
</ResponseField>

## Próximos passos

<CardGroup cols={2}>
  <Card title="Obter detalhes do projeto" icon="code" href="/pages/store/get">
    Consulte as configurações do projeto pela API.
  </Card>
  <Card title="Autenticação" icon="key" href="/pages/authentication">
    A chave de API define a qual projeto a requisição pertence.
  </Card>
</CardGroup>
